<?xml version="1.0" encoding="UTF-8" ?>
<Page
    x:Class="T3Code.App.Views.GitPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">
    <Grid Padding="16" RowSpacing="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Project:" VerticalAlignment="Center" FontSize="13" />
            <ComboBox x:Name="ProjectSelector" Grid.Column="1" MinWidth="200"
                      PlaceholderText="Select a project..." SelectionChanged="OnProjectSelected" />
            <Button x:Name="RefreshStatusBtn" Grid.Column="2" Content="Refresh"
                    Click="OnRefreshStatus" />
        </Grid>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Text="Branch:" VerticalAlignment="Center" FontSize="13" FontWeight="SemiBold" />
            <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                <TextBlock x:Name="BranchName" Text="(none)" VerticalAlignment="Center" FontSize="13" />
                <TextBlock x:Name="AheadBehindText" Text="" VerticalAlignment="Center" FontSize="12" Foreground="Gray" />
            </StackPanel>
        </Grid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" Spacing="8">
            <Button x:Name="ListBranchesBtn" Content="Branches" Click="OnListBranches" />
            <Button x:Name="PullBtn" Content="Pull" Click="OnPull" />
            <Button x:Name="InitBtn" Content="Init Repo" Click="OnInitRepo" />
        </StackPanel>

        <Grid Grid.Row="3">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Text="Changed Files" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8" />
            <ListView Grid.Row="1" x:Name="FileStatusList">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="4,2" ColumnSpacing="8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="60" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Text="{Binding StatusLabel}" FontSize="12"
                                       Foreground="{Binding StatusColor}" FontWeight="SemiBold"
                                       VerticalAlignment="Center" />
                            <FontIcon Grid.Column="1" Glyph="{Binding FileGlyph}" FontSize="14"
                                      VerticalAlignment="Center" />
                            <TextBlock Grid.Column="2" Text="{Binding Path}" FontSize="12"
                                       VerticalAlignment="Center" />
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
    </Grid>
</Page>
